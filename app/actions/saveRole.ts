"use server";

export async function saveRoleAction(puuid: string, role: string) {
  // Implement the logic to save the role
  console.log(`Saving role ${role} for puuid ${puuid}`);
  // Add your database or API call here
}
